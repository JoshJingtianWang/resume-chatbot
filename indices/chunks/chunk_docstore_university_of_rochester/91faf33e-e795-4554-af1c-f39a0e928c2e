{"lc": 1, "type": "constructor", "id": ["langchain", "schema", "document", "Document"], "kwargs": {"page_content": "agg_tbl <- ads %>%\n\ngroup_by(socialmedia, newcustomer) %>% summarise(customer_count = n(), .groups = \"drop\")\n\nyesrow <- agg_tbl[agg_tbl$newcustomer == \"yes\", ]$customer_count norow <- agg_tbl[agg_tbl$newcustomer == \"no\", ]$customer_count x <- matrix(c(yesrow, norow), nrow = 5, ncol = 2) chisq.test(x, correct = F)\n\n16\n\n## ## Pearson\u2019s Chi-squared test ## ## data: x ## X-squared = 31.799, df = 4, p-value = 2.103e-06\n\nYes, the Chi square test had a p-value less than 0.0001, so we reject the null hypothesis. Advertising on different social media platforms is associated with different rates of acquiring new customers. The plot below shows the different proportions of new customers on each platform.\n\nads %>%\n\ngroup_by(socialmedia) %>% summarize(n = n(), new_tot = sum(newcustomer == \"yes\"), new_prop = new_tot/n,\n\nold_tot = sum(newcustomer == \"no\"), old_prop = old_tot/n) %>%\n\nggplot(aes(x = socialmedia, y = new_prop)) + geom_col() + theme_classic() + ylab(\"Proportion of customers that are new\")\n\nFacebookInstagramTikTokTwitterYouTubesocialmediaProportion of customers that are new\n\n0.00.20.4\n\nPart 4C: Construct a two-sided 95% confidence interval for the proportion of ads that lead to new customers.\n\nyes <- nrow(ads[ads$newcustomer == \"yes\", ]) no <- nrow(ads[ads$newcustomer == \"no\", ]) total <- yes + no prop.test(yes, total)\n\n17\n\n## ## 1-sample proportions test with continuity correction ## ## data: yes out of total, null probability 0.5 ## X-squared = 18.921, df = 1, p-value = 1.363e-05 ## alternative hypothesis: true p is not equal to 0.5 ## 95 percent confidence interval: ## 0.3538487 0.4444248 ## sample estimates: p ## ## 0.3982869\n\nThe two-sided 95% confidence interval for the proportion of ads that lead to new customers is (0.354, 0.444).\n\nPart 4D: An analyst on another team claims that acquiring new customers is more profitable than trying to sell more products to existing customers. Test their claim at the \u03b1 = 0.05 significance level, and comment on your results.", "metadata": {"source": "./documents/University of Rochester/computational intro to stats final project report.pdf"}, "type": "Document"}}